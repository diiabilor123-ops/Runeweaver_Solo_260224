using UnityEngine;

public class BossCombat : EnemyCombat
{
    // 보스 전용 공격 로직 (예: 발도술 판정 범위)
    public void ExecuteDrawSword()
    {
        // 여기서 실제로 플레이어에게 데미지를 주는 로직 실행
        // 애니메이션 이벤트에서 호출하면 정확합니다.
    }
}